# URL da API de sync (PWA e build do frontend)
# Ex.: https://xxx.trycloudflare.com (Cloudflare Tunnel) ou http://localhost:3001 para testes
VITE_CLOUD_API_URL=https://interested-accessories-recognition-elections.trycloudflare.com
# Para o app Tauri (desktop), use os scripts:
#   npm run tauri:dev:cloud   - dev apontando para o VPS
#   npm run tauri:build:cloud - build do instalador apontando para o VPS
# Ou defina manualmente: TAURI_APP_CLOUD_API_URL=https://cold-layout-attribute-improvements.trycloudflare.com

# --- Variáveis do servidor (server/) - use em server/.env ou ao rodar o servidor ---
# JWT_SECRET=sua-chave-secreta  (obrigatório em produção)
# CORS_ORIGIN=https://gestorfinanceirosimhal.netlify.app  (vírgulas para vários; Netlify e localhost já incluídos por padrão)
# RATE_LIMIT_MAX_AUTH=20  RATE_LIMIT_MAX_SYNC=200  (opcional; ver server/.env.example)
