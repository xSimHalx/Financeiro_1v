# PWA: defina false para desabilitar Service Worker (evita cache antigo em deploy)
# VITE_PWA_ENABLED=false

# URL da API backend (auth, sync, health). Obrigatório em produção.
# Produção: https://api.simhal.tech
# Dev local: http://localhost:3001
VITE_API_URL=https://api.simhal.tech

# App Tauri (desktop): npm run tauri:dev:cloud / tauri:build:cloud
# TAURI_APP_CLOUD_API_URL=https://api.simhal.tech

# --- Variáveis do servidor (server/) - use em server/.env ou ao rodar o servidor ---
# JWT_SECRET=sua-chave-secreta  (obrigatório em produção)
# CORS_ORIGIN=https://gestorfinanceirosimhal.netlify.app  (vírgulas para vários; Netlify e localhost já incluídos por padrão)
# RATE_LIMIT_MAX_AUTH=20  RATE_LIMIT_MAX_SYNC=200  (opcional; ver server/.env.example)
