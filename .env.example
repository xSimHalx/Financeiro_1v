# PWA: defina false para desabilitar Service Worker (evita cache antigo em deploy)
# VITE_PWA_ENABLED=false

# URL da API backend. DEV usa .env (localhost). PROD usa .env.production ou variável no build.
# Dev: http://localhost:3000  (ou 3001 se o server rodar em outra porta)
# Prod: https://simhal.tech
VITE_API_URL=https://simhal.tech

# App Tauri (desktop): npm run tauri:dev:cloud / tauri:build:cloud
# TAURI_APP_CLOUD_API_URL=https://simhal.tech

# --- Variáveis do servidor (server/) - use em server/.env ---
# JWT_SECRET, CORS_ORIGIN (Hostinger + simhal.tech). Ver server/.env.example
