# URL da API de sync (PWA e build do frontend)
# Ex.: https://sua-api.railway.app ou http://localhost:3001 para testes
VITE_CLOUD_API_URL=http://localhost:3001

# Para o app Tauri (desktop), defina ao rodar:
#   Windows: $env:TAURI_APP_CLOUD_API_URL="http://localhost:3001"; npm run tauri dev
#   macOS:   TAURI_APP_CLOUD_API_URL="http://localhost:3001" npm run tauri dev
# O build do app embute o valor do ambiente; para produção use a URL real da API.

# --- Variáveis do servidor (server/) - use em server/.env ou ao rodar o servidor ---
# JWT_SECRET=sua-chave-secreta  (obrigatório em produção)
# CORS_ORIGIN=https://app.vertexads.com  (ou http://localhost:5173 para dev; vírgulas para vários)
# RATE_LIMIT_MAX_AUTH=20  RATE_LIMIT_MAX_SYNC=200  (opcional; ver server/.env.example)
